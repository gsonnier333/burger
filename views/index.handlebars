<h1>Burgers</h1>

<h2>Add a burger:</h2>
<form id="addForm">
    <input type="text" placeholder="Burger name" id="newBurgerName"></textarea>
    <button type="submit">Add</button>
</form>



<h2>Uneaten burgers:</h2>
<ul>
    {{#each burgers}}
    {{#unless devoured}}
    <li>
        {{burger_name}}
        <button id="{{id}}" class="eatBtn">Eat!</button>
    </li>
    {{/unless}}
    {{/each}}
</ul>

<h2>Devoured burgers:</h2>
<ul>
    {{#each burgers}}
    {{#if devoured }}
    <li>
        {{burger_name}}
    </li>
    {{/if}}
    {{/each}}
</ul>

<p>🍔</p>